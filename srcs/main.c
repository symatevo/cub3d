#include "../Includes/cub3d.h"

int     main(int argc, char **argv)
{
    t_world     world;
    t_mlx       mlx;

    //window_create(&mlx);
    parsing(argc, argv);
    //main_algorithm(&world);
    //memory_clear();
}
